/**
 * Environment Configuration
 * 
 * This module provides access to environment variables and configuration.
 * Uses Expo Constants to access app.config values.
 */

// Placeholder - will be populated when migrating from lib/
export const ENV = {
    // These will be populated from Expo Constants/app.config
    SUPABASE_URL: '',
    SUPABASE_ANON_KEY: '',
} as const;
